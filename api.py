# import requests
# import json
#
# URL = f'https://min-api.cryptocompare.com/data/price?fsym={fsym}&tsyms={fsyms}'
# r = requests.get(URL)
# resp = json.loads(r.content)
# new_price = resp[]

exchanges = {
    'доллар': 'USD',
    'евро': 'EUR',
    'рубль': 'RUB'
}

